# Supabase — projeto System-FozGo
# O painel usa a service_role SOMENTE no servidor. NUNCA faça commit de valores reais.

SUPABASE_URL=https://nnsperehtnqwpbwugjxb.supabase.co

# Settings → API → Project API keys → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=cole-aqui-a-service-role-key

# Settings → API → Project API keys → anon (public) — usada no login/sessão
SUPABASE_ANON_KEY=cole-aqui-a-anon-key

# E-mails com acesso ao painel (separados por vírgula)
ADMIN_EMAILS=voce@exemplo.com
